---
# files that should be pinned to the top like reference
# are prefixed with a @ symbol
title: 🌟 Empezando
description: Conceptos básicos de la API
---

Nuestra API es HTTPS/REST para operaciones generales como enviar solicitudes 'POST'.

#### URL base

[https://fred.hidencloud.com:25736/v1](https://fred.hidencloud.com:25736/v1) (Esta url cambiará en el futuro)

## Método HTTP

• POST: Utiliza este método para enviar un mensaje y recibir una respuesta.

▎Cuerpo de la Solicitud

```json:title=HTTP%20Headers
{
  "prompt": "Tu mensaje aquí"
}
```

• prompt: (string) El mensaje que deseas enviar a la IA.

▎Respuesta

La respuesta de la API será un objeto JSON que contiene:

```json:title=HTTP%20Headers
{
  "response": "Respuesta generada por la IA"
}
```

• response: (string) La respuesta generada por el modelo de IA.

▎Ejemplo de Solicitud

```
curl -X POST http://localhost:25736/v1 -H "Content-Type: application/json" -d '{"prompt": "¿Cómo estás?"}'
```

▎Ejemplo de Respuesta

```json:title=HTTP%20Headers
{
  "response": "¡Estoy bien, gracias! ¿Y tú?"
}
```
